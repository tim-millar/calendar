import React from "react"
import PropTypes from "prop-types"
class Foo extends React.Component {
  render () {
    return (
      <React.Fragment>
        Foo Bar
      </React.Fragment>
    );
  }
}

export default Foo
